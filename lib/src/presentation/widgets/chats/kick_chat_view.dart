import 'package:flutter/material.dart';

class KickChatView extends StatelessWidget {
  const KickChatView({
    super.key,
    required this.chatroomId,
  });

  final String chatroomId;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}